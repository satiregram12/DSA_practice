// Title: Pow(x, n)
            // Difficulty: Medium
            // Language: Java
            // Link: https://leetcode.com/problems/pow(x,-n)/

class Solution {
    public double myPow(double x, int n) {
        double p = Math.pow(x,n);
    }
        return p; 
}
